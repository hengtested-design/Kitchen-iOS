# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode — 编译产物 & 用户态
build/
DerivedData/
*.xcarchive
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/
*.xcuserstate
*.xcuserdatad/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.hmap
*.xccheckout

# Xcode 自动生成的 pbxproj 备份（Edit History)
*.pbxproj.bak.*
*.mode1v3
*.pbxuser

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# CocoaPods（这个项目没用到）
Pods/

# IDE — IntelliJ
.idea/

# 项目本地数据 & 中间产物
screenshots/

# 数据 / 爬虫目录（各自独立 repo / 数据集，不进入 iOS 代码库）
distribution/
crawl/

# 调试编译产物（不是 DerivedData，是项目里手动 build/ 出来的）
builds/

# .json 从 .yaml 生成（Xcode Run Script Phase - "Sync YAML to JSON" 自动补上）
# 不进 git，导致 Office git 状态干净。
Kitchen/Recipes/**/*.json

# 临时文件
*.tmp
*.bak
*.swp

# probe_image_status.py 生成的运行时状态（每次跑都更新，不应进 git）
scripts/image_status.json
scripts/image_status.diff.json
